# /lib/design_system

一句话：设计系统相关代码（设计令牌等）；供 UI 层统一管理视觉与交互的基础数值。

## 文件清单
- `design_tokens.dart` - `ChatBoxTokens` 设计令牌（间距/圆角/阴影/动画/断点）

## 备注
- `apple_icons.dart` 已移除：仓库内无运行时引用（仅文档/脚本历史提及），为降低维护成本做减法。

