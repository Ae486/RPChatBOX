# Runtime: API / gateway
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Runtime: upstream LLM access
httpx>=0.26.0

# Runtime: unified LLM provider gateway
litellm>=1.50.0
google-genai>=1.33.0
markitdown[all]
sqlmodel>=0.0.22
psycopg[binary]>=3.2.0
langgraph-checkpoint-postgres>=2.0.0

# Test
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Note:
# - MarkItDown is used for backend-side document parsing during Phase 1.5.
# - SSE helper frameworks are intentionally excluded for now; the current
#   backend uses FastAPI StreamingResponse directly.
