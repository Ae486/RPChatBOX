# Code Quality Audit Analysis - Constraint Generation Request

## Context
You are analyzing a Flutter LLM client app (ChatBoxApp) with ~45,000 lines of code.
A comprehensive code quality audit has been completed, identifying critical technical debt.

## CRITICAL ISSUES (8 total, must fix immediately)

### 1. Rendering State Machine Bugs (lib/rendering/markdown_stream/stable_prefix_parser.dart)
- **C1**: Fence detection 4+ backtick bug (Line 48) - treats 4+ backticks as 3, causing premature fence close
- **C2**: Table mode lock bug (Lines 110, 214) - single pipe row marks tableMode=1 but never releases, streaming stalls
- **C3**: HTML stack tracking broken (Lines 89, 259) - `<div></div>` on one line pushes but never pops, all subsequent content marked unstable
- **C4**: HTML not gating fence/table (Lines 83, 110, 48) - fence/table detection runs inside HTML blocks, state machine confusion

### 2. Provider Layer Crashes (lib/adapters/ai_provider.dart)
- **C5**: DeepSeek/Claude Provider placeholder throws UnimplementedError (Lines 206, 221, 262) - APP CRASH when user selects these providers

### 3. Initialization & Lifecycle Bugs
- **C6**: ChatSessionProvider async init timing bomb (lib/providers/chat_session_provider.dart:32-34) - constructor calls async _init() without await, public methods may execute before Hive ready
- **C7**: Uri.tryParse()! force unwrap (lib/pages/provider_detail_page.dart:366) - RUNTIME EXCEPTION when user types invalid URL
- **C8**: setState after dispose (lib/pages/*.dart, lib/widgets/*.dart) - YELLOW SCREEN ERROR on fast navigation

## HIGH PRIORITY ISSUES (20+ total)

### Performance
- Base64 decode repeated in build() - GC spikes, scroll jank
- Code highlight sync blocking UI - multiple code blocks cause jank
- O(n²) string concatenation in ChunkBuffer

### Resource Leaks
- ChatSessionProvider no dispose() - HiveConversationService.close() never called
- Dialog TextEditingController leak - controllers in showDialog() not disposed
- Toast timer not cancelled - stale toasts override new ones

### Code Quality
- 40+ silent catch(_) blocks - debugging impossible
- Code duplication: metadata handling in 3+ places
- Oversized files: streaming.dart (972 lines), provider_detail_page.dart (845 lines)
- ApiError class duplicated (models/ vs adapters/)

## YOUR TASK

Generate a structured constraint set for fixing these issues:

1. **Prioritized Fix Groups** - Group issues by dependency and risk
2. **Fix Sequence** - What must be fixed before what (dependency graph)
3. **Architectural Patterns** - Recommend patterns for each fix:
   - State machine fixes: Guard clauses, State pattern
   - Provider fixes: Factory pattern, Strategy pattern
   - Lifecycle fixes: Dispose guards, mounted checks
   - Performance fixes: Caching, lazy loading, async processing
4. **Test Coverage Requirements** - What tests must accompany each fix
5. **Risk Assessment** - What could break during fixes

Output format:
```
## Constraint Set: [Name]
Priority: P0/P1/P2
Issues: C1, C2, ...
Dependencies: [what must be fixed first]
Pattern: [architectural pattern]
Tests Required: [test types]
Risk: [what could break]
Implementation Constraints:
- [specific constraint 1]
- [specific constraint 2]
```
